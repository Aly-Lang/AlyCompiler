gcd_euclid : integer (a : integer b : integer) = integer (a : integer b : integer) {
  if a = b {
    a
  } else {
    if a < b {
      gcd_euclid(a, b - a)
    } else {
      gcd_euclid(a - b, b)
    }
  }
}

16 / 4

gcd_euclid(88, 32)