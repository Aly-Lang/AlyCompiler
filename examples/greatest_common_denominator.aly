defun gcd_euclid (a : integer, b : integer) : integer {
    if a = b {
        debug_marker : integer
        a
    } else {
        debug_marker
        if a < b {
           gcd_euclid(a, b - a)
        } else {
            gcd_euclid(a - b, b)
        }
    }   
}

gcd_euclid(88, 32)