; We now have syntax highlighting of comments in Emacs :^)

defun foo () : integer {
    a : integer = 0

    ptr : @integer
    ptr := &a
    @ptr := 4
    
    a
}

defun bar (ptr : @integer) : integer {
    0
}

foo()
